import React from 'react';
import PropTypes from 'prop-types';

const Language = ({}) => {
  return (
    <div>
      <p>Span</p>
    </div>
  );
};

Language.propTypes = {};

export default Language;
